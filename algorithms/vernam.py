def numberfy(letters):
    meow= letters.lower()
    nums = []
    for m in meow:
        nums.append(ord(m)-97)
    return nums

def encrypt(message,key):
    return

def decrypt(message,key):
    return

plaintext = "HELLO"
key = "OCVHW"